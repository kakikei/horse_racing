<%= form_with(model: horse_detail) do |form| %>
  <% if horse_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(horse_detail.errors.count, "error") %> prohibited this horse_detail from being saved:</h2>

      <ul>
        <% horse_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
